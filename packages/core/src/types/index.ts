/**
 * 全局类型桶导出
 *
 * 全局枚举见 './enums'（enums/），本入口仅导出接口与 type 别名类型。
 */
export { type TargetPlatform, type PlatformChoice } from '@/types/platform'
export { type CssPreprocessor, type StateManager, type RouterScheme, type PackageManager, type CreateOptions, type TemplateContext, type QuestionPresets } from '@/types/options'
